.row
  .col.s6.offset-s3
    %br
    %br
    .card.blue-grey.darken-3.white-text.z-depth-3
      = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
        = devise_error_messages!
        .card-content
          .input-field
            = f.label :email
            %br/
            = f.email_field :email, autofocus: true
          .input-field
            = f.label :password
            %br/
            = f.password_field :password, autocomplete: "off", placeholder: "#{@minimum_password_length} characters minimum"
          .input-field
            = f.label :password_confirmation
            %br/
            = f.password_field :password_confirmation, autocomplete: "off"
          %br
          %button.waves-effect.waves-light.btn-flat.teal.lighten-1{type: 'submit'} Sign up
    = render "devise/shared/links"
